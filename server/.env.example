# Copie este arquivo para ".env" (mesma pasta) e preencha com sua chave real.
# NUNCA envie o arquivo ".env" para o git — ele já está no .gitignore.

ANTHROPIC_API_KEY=sk-ant-coloque-sua-chave-aqui

# Porta local do servidor (opcional, padrão 3000)
PORT=3000
